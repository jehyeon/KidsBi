structure (QuizResult) {
  description (StartQuiz's output)
  property (quizInfos) {
    type (QuizInfo)
    min (Required) max (One)
  }
  property (quizCount) {
    type (QuizCount)
    min (Required) max (One)
  }
}
