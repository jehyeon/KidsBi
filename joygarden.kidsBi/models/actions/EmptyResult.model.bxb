action (EmptyResult) {
  type (Fetch)
  description (Crate a false array of quizInfos.length)
  collect {
    input (quizProgress) {
      type (QuizProgress)
      min (Required) max (One)
    }
  }
  output (viv.core.Boolean)
}
