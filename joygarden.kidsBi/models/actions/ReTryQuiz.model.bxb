action (ReTryQuiz) {
  type (UpdateTransaction)
  description (__DESCRIPTION__)
  collect {
    input (quizProgress) {
      type (QuizProgress)
      min (Required) max (One)
    }
  }
  output (QuizProgress)
}
