layout {
  match: VideoInfo (videoInfo)
  mode (Details)

  content {
    section {
      content {
        video {
          url ("#{value(videoInfo.videoFile)}")
          // poster ()
        }
      }
    }
  }
}
